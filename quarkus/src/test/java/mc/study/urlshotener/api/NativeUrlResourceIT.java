package mc.study.urlshotener.api;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeUrlResourceIT extends UrlResourceTest {

    // Execute the same tests but in native mode.
}